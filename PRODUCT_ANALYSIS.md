# Аналитика и план улучшений проекта Bura Mini App

**Дата анализа**: 2025-12-18
**Версия проекта**: 0.0.3

---

## 📊 Текущее состояние проекта

### Архитектура
- **Frontend**: React 18 + Vite + TypeScript
- **Backend**: FastAPI + WebSocket (Python)
- **Платформа**: Telegram Mini App
- **Деплой**: Docker + GitHub Pages
- **Хранение данных**: In-memory (без БД)

### Реализованный функционал

#### ✅ Работает хорошо:
1. **Игровая механика**: Полная реализация правил игры Бура для 2-4 игроков
2. **WebSocket коммуникация**: Реал-тайм обновления состояния игры
3. **Базовый reconnect**: Автоматическое переподключение WebSocket с fallback на HTTP polling
4. **UI/UX**: Адаптивный интерфейс для Telegram WebApp
5. **Аутентификация**: Безопасная проверка через Telegram InitData

#### ⚠️ Требует улучшения:
1. **Reconnect при перезагрузке**: Игрок НЕ возвращается в комнату при закрытии/перезагрузке приложения
2. **Отсутствие БД**: Все данные теряются при рестарте сервера
3. **Нет глобальной статистики**: Отсутствует история игр и рейтинг игроков
4. **Нет раздела "Игроки"**: Невозможно посмотреть статистику и рейтинг

---

## 🔍 Выявленные проблемы

### 🔴 Критические проблемы

#### 1. **Неполный механизм reconnect**
**Локация**: `frontend/src/room-channel.ts:69-106`, `frontend/src/App.tsx:263-291`

**Проблема**:
- При закрытии вкладки/приложения игрок теряет связь с комнатой навсегда
- При перезагрузке страницы нет восстановления активной сессии
- `roomId` хранится только в state компонента (теряется при unmount)

**Влияние**: Пользователь вынужден создавать новую игру при любом обрыве

**Текущая реализация**:
```typescript
// room-channel.ts - есть переподключение WebSocket
function scheduleReconnect() {
  reconnectAttempts++
  const delay = Math.min(8000, 500 * 2 ** reconnectAttempts)
  reconnectTimer = setTimeout(() => connect(), delay)
}
```

**Что не хватает**:
- Сохранение `roomId` и `playerId` в localStorage
- Проверка активной комнаты при старте приложения
- API для валидации сессии игрока в комнате

#### 2. **Отсутствие персистентного хранилища**
**Локация**: `backend/game.py:826`

**Проблема**:
```python
ROOMS: Dict[str, Room] = {}  # In-memory словарь теряется при рестарте
```

**Последствия**:
- Все активные игры прерываются при обновлении сервера
- Невозможно хранить историю матчей
- Нет данных для расчёта рейтинга

#### 3. **Нет раздела "Игроки"**
**Проблема**: Отсутствует UI и backend логика для:
- Глобального рейтинга игроков
- Статистики (количество игр, побед, поражений)
- Истории матчей

---

### 🟡 Важные недоработки

#### 4. **UX при потере соединения**
**Проблема**:
- Нет визуального индикатора состояния подключения
- Пользователь не понимает, когда он offline
- Нет уведомлений о переподключении

**Локация**: `frontend/src/App.tsx` - отсутствует UI компонент

#### 5. **Удаление пустых комнат**
**Локация**: `backend/main.py:152-158`

**Проблема**:
```python
if len(ROOMS[rid].players) == 0:
    ROOMS.pop(rid, None)
```

При кратковременном обрыве у всех игроков комната удаляется автоматически.

**Решение**: Отложенное удаление (grace period 5-10 минут)

#### 6. **Отсутствие модерации**
- Нет контроля за созданием комнат (лимиты на пользователя)
- Нет механизма бана токсичных игроков
- Нет репорта о нарушениях

---

## 🎯 План улучшений с приоритетами

### 🔥 Критический приоритет (P0)

#### ✅ 1. Улучшенный механизм reconnect
**Цель**: Игрок может вернуться в комнату при обрыве сети или перезагрузке приложения

**Задачи**:
1. **Frontend**:
   - Сохранять `roomId` и `playerId` в `localStorage` при входе в комнату
   - При старте приложения проверять наличие активной сессии
   - Автоматически переподключаться к сохранённой комнате
   - Добавить UI индикатор статуса подключения

   **Файлы**: `frontend/src/App.tsx`, `frontend/src/room-channel.ts`

2. **Backend**:
   - Не удалять игрока сразу при `WebSocketDisconnect`
   - Помечать игрока как "отключен" и давать grace period (5 мин)
   - Добавить API `/api/game/rejoin/{room_id}` для проверки возможности возврата
   - При повторном подключении восстанавливать состояние

   **Файлы**: `backend/main.py`, `backend/game.py`

3. **Тесты**:
   - Сценарий: закрытие и открытие приложения
   - Сценарий: обрыв сети на 30 секунд
   - Сценарий: перезагрузка страницы во время игры

**Оценка**: 3-4 часа разработки

---

### 🔥 Высокий приоритет (P1)

#### 📊 2. Интеграция базы данных
**Цель**: Сохранение статистики игроков и истории матчей

**Выбор БД**: **SQLite** (простота, не требует отдельного сервиса)

**Схема данных**:
```sql
-- Игроки
CREATE TABLE players (
    player_id TEXT PRIMARY KEY,
    name TEXT NOT NULL,
    avatar_url TEXT,
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    last_seen TIMESTAMP
);

-- Статистика игроков
CREATE TABLE player_stats (
    player_id TEXT PRIMARY KEY,
    total_matches INTEGER DEFAULT 0,
    wins INTEGER DEFAULT 0,
    losses INTEGER DEFAULT 0,
    rating INTEGER DEFAULT 1000,
    FOREIGN KEY (player_id) REFERENCES players(player_id)
);

-- История матчей
CREATE TABLE matches (
    match_id TEXT PRIMARY KEY,
    room_id TEXT,
    variant_key TEXT,
    started_at TIMESTAMP,
    finished_at TIMESTAMP,
    winner_id TEXT,
    total_rounds INTEGER
);

-- Участники матчей
CREATE TABLE match_participants (
    match_id TEXT,
    player_id TEXT,
    final_score INTEGER,
    is_winner BOOLEAN,
    FOREIGN KEY (match_id) REFERENCES matches(match_id),
    FOREIGN KEY (player_id) REFERENCES players(player_id),
    PRIMARY KEY (match_id, player_id)
);
```

**Задачи**:
1. Добавить `aiosqlite` в `backend/requirements.txt`
2. Создать `backend/database.py` с ORM-like wrapper
3. Инициализация БД при старте приложения
4. Сохранять результаты матча при завершении игры
5. Обновлять статистику игроков

**Конфигурация**:
```python
# backend/.env
DATABASE_URL=sqlite:///./data/bura.db  # Вынести в переменную окружения
```

**Файлы**:
- Новый: `backend/database.py`
- Изменения: `backend/game.py:_finalize_round()` (строка 686)
- Изменения: `backend/main.py` (добавить инициализацию БД)

**Оценка**: 4-5 часов разработки

---

#### 🏆 3. Раздел "Игроки"
**Цель**: Показать рейтинг и статистику игроков

**Backend API**:
```python
# Новые эндпоинты
GET /api/players/leaderboard?limit=50  # Топ игроков по рейтингу
GET /api/players/{player_id}/stats     # Статистика конкретного игрока
GET /api/players/{player_id}/history   # История матчей игрока
```

**Frontend компонент**:
```tsx
// Новый файл: frontend/src/components/Leaderboard.tsx
interface LeaderboardEntry {
  rank: number
  playerId: string
  name: string
  avatarUrl?: string
  rating: number
  totalMatches: number
  wins: number
  losses: number
  winRate: number  // wins / totalMatches * 100
}
```

**UI структура**:
```
Главное меню (MainMenu.tsx)
├── Новая игра
├── Присоединиться
├── Игроки ← НОВАЯ КНОПКА
└── Правила

Экран "Игроки" (PlayersScreen.tsx)
├── Табы:
│   ├── Рейтинг (Leaderboard)
│   └── Моя статистика (MyStats)
└── Список игроков:
    ├── #1 Иван - 1250 рейтинг - 45 игр - 30 побед (67%)
    ├── #2 Мария - 1180 рейтинг - 38 игр - 25 побед (66%)
    └── ...
```

**Расчёт рейтинга** (упрощённый Elo):
```python
def calculate_rating_change(winner_rating: int, loser_rating: int, k: int = 32) -> tuple[int, int]:
    """
    Возвращает (изменение для победителя, изменение для проигравшего)
    """
    expected_win = 1 / (1 + 10 ** ((loser_rating - winner_rating) / 400))
    change = int(k * (1 - expected_win))
    return change, -change
```

**Задачи**:
1. Backend: API для получения рейтинга и статистики
2. Frontend: Компонент `Leaderboard.tsx`
3. Frontend: Компонент `PlayerStats.tsx`
4. Frontend: Добавить навигацию в `MainMenu.tsx`
5. Интеграция с БД

**Файлы**:
- Новый: `backend/api_players.py`
- Новый: `frontend/src/components/Leaderboard.tsx`
- Новый: `frontend/src/components/PlayerStats.tsx`
- Изменения: `frontend/src/App.tsx` (добавить screen 'players')
- Изменения: `frontend/src/components/MainMenu.tsx` (кнопка "Игроки")

**Оценка**: 5-6 часов разработки

---

### 🟢 Средний приоритет (P2)

#### 4. **UX улучшения**
- Индикатор состояния подключения (онлайн/оффлайн/переподключение)
- Уведомления о событиях (игрок присоединился, покинул игру)
- Анимации переходов
- Звуковые эффекты (опционально)

**Оценка**: 2-3 часа

#### 5. **Grace period для комнат**
- Не удалять пустые комнаты сразу
- Отложенное удаление через 10 минут

**Оценка**: 1 час

#### 6. **Улучшение логирования**
- Структурированные логи (JSON)
- Tracking игровых событий
- Мониторинг reconnect rate

**Оценка**: 2 часа

---

### 🔵 Низкий приоритет (P3)

#### 7. **Социальные функции**
- Добавление друзей
- Приглашения в игру
- Чат в комнате

#### 8. **Продвинутая статистика**
- Графики прогресса рейтинга
- Любимые комбинации карт
- Средняя длительность игры

#### 9. **Модерация**
- Система репортов
- Бан токсичных игроков
- Лимиты на создание комнат

---

## 📋 Итоговый план реализации

### Этап 1: Критические улучшения (1 неделя)
1. ✅ **Reconnect при перезагрузке** (P0)
   - Локальное хранение сессии
   - Grace period для игроков
   - API для повторного входа
2. ✅ **Интеграция SQLite** (P1)
   - Схема БД
   - Сохранение статистики

### Этап 2: Раздел "Игроки" (1 неделя)
3. ✅ **Backend API** (P1)
   - Эндпоинты для рейтинга
   - Расчёт рейтинга по Elo
4. ✅ **UI компоненты** (P1)
   - Leaderboard
   - Статистика игрока

### Этап 3: UX полировка (опционально)
5. UX улучшения (P2)
6. Grace period для комнат (P2)

---

## 🛠 Технические требования

### Зависимости (добавить в requirements.txt)
```txt
aiosqlite==0.19.0
```

### Переменные окружения (.env)
```bash
# Существующие
BOT_TOKEN=123456:ABC-NEW-TOKEN
ORIGIN=http://localhost:5173

# Новые
DATABASE_URL=sqlite:///./data/bura.db
RECONNECT_GRACE_PERIOD_SEC=300  # 5 минут
```

### Миграции БД
Рекомендуется использовать простые SQL миграции (без ORM), так как проект небольшой.

---

## ✅ Критерии завершения задачи

### 1. Reconnect (P0)
- [ ] Игрок может закрыть приложение и вернуться в комнату в течение 5 минут
- [ ] При перезагрузке страницы игрок автоматически возвращается в активную игру
- [ ] Видимый индикатор статуса подключения

### 2. База данных (P1)
- [ ] Все матчи сохраняются в БД
- [ ] Статистика обновляется после каждой игры
- [ ] Переменная DATABASE_URL вынесена в .env

### 3. Раздел "Игроки" (P1)
- [ ] Работает страница с рейтингом топ-50 игроков
- [ ] Отображается статистика: матчи, победы, поражения, рейтинг
- [ ] Матч считается завершённым при достижении 12 очков одним из игроков

---

## 🚀 Рекомендации по приоритизации

**Немедленно реализовать** (блокирует пользовательский опыт):
1. Reconnect при перезагрузке
2. Интеграция БД

**Реализовать в следующем спринте** (добавляет ценность):
3. Раздел "Игроки"

**Можно отложить** (nice-to-have):
4. UX улучшения
5. Социальные функции

---

## 📈 Метрики успеха

После внедрения улучшений отслеживать:
- **Reconnect rate**: % успешных возвратов в игру
- **Retention**: % игроков, вернувшихся через 24 часа
- **Average session time**: средняя длительность сессии
- **Completed matches**: % завершённых игр (не покинутых)

---

## 🔗 Ссылки на код

### Критические файлы для изменения:
- `frontend/src/App.tsx` - основная логика приложения
- `frontend/src/room-channel.ts` - WebSocket управление
- `backend/main.py` - API эндпоинты и WebSocket хаб
- `backend/game.py` - игровая логика
- `backend/models.py` - модели данных

### Новые файлы:
- `backend/database.py` - работа с БД
- `backend/migrations/001_initial.sql` - схема БД
- `frontend/src/components/Leaderboard.tsx` - рейтинг
- `frontend/src/components/PlayerStats.tsx` - статистика игрока

---

**Документ составлен**: 2025-12-18
**Автор анализа**: Claude (AI Assistant)
**Следующий шаг**: Реализация улучшенного reconnect механизма
